%p#notice= notice

%h1 Pokemon

= form_with url: pokemon_index_path, method: :get, local: true do |f|
  = f.label :name
  = f.text_field :name
  = f.submit t(".search")
= link_to t(".advanced_search"), search_pokemon_index_path
%br
%br

- if @pokemons.empty?
  %p No Pokemon were found.
- else
  %table
    %thead
      %tr
        %th= t(".name")
        %th= t(".type")
        %th= t(".region")
        %th{colspan: "3"}
    = render @pokemons
%br

= link_to t(".new_pokemon"), new_pokemon_path
